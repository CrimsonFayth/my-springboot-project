# Spring Boot Application

A sample Spring Boot application with JPA and H2 database.

## Building

```bash
mvn clean install
```

## Running

```bash
mvn spring-boot:run
```

The application will start on `http://localhost:8080`
